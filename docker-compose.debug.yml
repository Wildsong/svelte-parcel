version: '3.4'

services:
  svelteparcel:
    image: svelteparcel
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 1234:1234
    volumes:
      # Parcel needs to write 
      - ./src:/srv:rw
